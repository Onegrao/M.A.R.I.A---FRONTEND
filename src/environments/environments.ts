export const environment = {
  production: false,
  // Endereço base para o backend principal (Django), incluindo o prefixo /api
  apiUrl: 'http://localhost:8000/api',

  // Endereço para o serviço do Chatbot (FastAPI)
  chatbotApiUrl: 'http://localhost:8001'
};

